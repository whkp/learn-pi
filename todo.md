# Learn Pi 改造计划

> 目标：在保留“准确、离线、可验证、安全边界清晰”优势的基础上，把 Learn Pi 从“机制解读 + 单元实验”升级为“可连续学习、可运行、可对照源码、可完成项目”的 Agent Harness 工程课程。
>
> 参考对象：
> - `learning/dg-ai-notes`：源码精读、TypeScript/Python 双轨、交互式图表和沉浸式阅读。
> - `learning/learn-agent-the-hard-way`：连续练习、同一代码库逐步演进、真实运行结果和完整产品能力。

## 0. 当前基线与总体目标

### 当前基线

- 11 个主题章节，文档约 1,400 行。
- Python 标准库离线实验，实验/项目/测试约 4,500 行。
- 107 个单元测试。
- 已有课程契约检查、Markdown 链接检查、Pi 0.84.2 源码映射。
- 当前文档源在 `docs/`，通过 `scripts/build_site.py` 生成 mdBook 的 `src/`。
- 当前章节以“是什么 / 怎么做 / 为什么 / 实验 / 验证 / 回顾”为主要结构。

### 目标状态

- 一条从裸 API 到完整 Harness 的连续编码主线。
- 每个机制同时具备：概念解释、源码证据、最小实验、失败实验、迁移练习。
- Python 离线模式和 TypeScript/Pi 真实 API 形成明确对照。
- 至少一个贯穿全书的完整 Coding Agent 项目。
- 页面支持章节导航、代码复制/折叠/行号、双版本切换、图表放大和学习进度。
- CI 能验证文档、代码、链接和站点构建，读者能在干净环境中复现。

### 非目标

- 不把仓库改造成 Pi SDK 的 Python 绑定。
- 不复制 Pi 全部生产代码或重新维护一套完整 API 文档。
- 不默认调用真实模型，不把 API Key 写进仓库。
- 不因为增加交互而牺牲静态 Markdown、PDF 和离线阅读能力。

---

## 1. P0：先修正课程契约与可用性

> 目标：让现有课程的概念、构建和首次体验可靠。完成后再扩充内容。

### P0-01 修正 Agent Loop 终止语义

- [ ] 修改 `docs/02-agent-loop/README.md` 中“`stopReason` 是唯一终止信号”的表述。
- [ ] 改为：`stopReason` 是模型调用层的重要状态，最终继续/终止还取决于 tool calls、`terminate`、pending messages、steering/follow-up、取消和错误。
- [ ] 增加“模型 API 返回值”和“框架注入状态”的对照表。
- [ ] 增加一个包含 `stopReason=length` 但存在工具调用的边界案例，并说明实际 Pi 行为。
- [ ] 在 `docs/pi-source-map.md` 增加对应源码符号和 permalink。
- 验收：章节中的伪代码与 Pi 0.84.2 基线行为一致；新增测试覆盖 stop、toolUse、length、error、aborted、terminate。

### P0-02 建立统一章节模板

- [ ] 为 11 个现有章节统一以下小节顺序：
  1. 本章要解决的问题
  2. 学习目标
  3. 一分钟全景
  4. 核心概念
  5. Pi 源码证据
  6. Python 教学实验
  7. 失败与边界实验
  8. 动手任务
  9. 生产迁移注意事项
  10. 验证命令
  11. 回顾与下一章
- [ ] 每章开头增加“预计时间、前置章节、输入、产出”。
- [ ] 每章结尾增加“完成检查清单”和“常见卡点”。
- [ ] 将现有章节中的重复说明抽到 `docs/glossary.md` 或公共说明页。
- 验收：每章都有可扫描的目标、代码入口、运行命令、预期结果和下一步链接。

### P0-03 修复首次构建路径

- [ ] 在 `README.md` 明确 `docs/` 是单一事实源，发布前必须先运行 `python3 scripts/build_site.py`。
- [ ] 增加 `scripts/build_and_check.sh` 或等价 Python 入口，串联：生成 `src/`、课程契约、链接检查、单元测试、mdBook build。
- [ ] 在 `.github/workflows/` 增加 CI 构建任务。
- [ ] 明确 `src/` 是否纳入 git；推荐继续忽略生成目录，并在 CI 中生成。
- [ ] 构建失败时输出具体文件和阶段，而不是只返回非零退出码。
- 验收：在干净 checkout 中执行单条命令即可完成检查和构建。

### P0-04 增加代码块基础增强

- [ ] 继续使用 mdBook，先不迁移站点框架。
- [ ] 在 `theme/head.hbs` 或 `theme/custom.css` 配合少量 JS 增加代码复制按钮。
- [ ] 增加语言标签、文件路径、行号和 GitHub 源码链接。
- [ ] 超过 30 行的代码默认折叠，必须保留键盘可访问的展开按钮。
- [ ] 代码块下方统一显示“运行命令”和“预期输出”。
- [ ] 长命令和输出支持横向滚动，不挤压正文宽度。
- 验收：桌面和手机端代码不溢出；复制成功/失败有明确状态；`prefers-reduced-motion` 下无过渡动画。

### P0-05 重做首页信息架构

- [ ] 重写 `docs/00-course-map.md` 首页首屏，突出三件事：课程学什么、适合谁、下一步读什么。
- [ ] 保留章节网格，但在其前增加三条入口：
  - 概念路线：按章节顺序阅读。
  - 实验路线：直接运行离线 Lab。
  - 源码路线：按 Pi 源码入口追踪。
- [ ] 增加阶段进度条或状态摘要：基础机制 / 状态边界 / 集成实践 / 综合项目。
- [ ] 增加“推荐下一章”模块，避免读者面对 11 个同等权重入口。
- [ ] 将首页中的 ASCII 总图逐步替换为可缩放 SVG，同时保留纯文本 fallback。
- 验收：新用户从首页 10 秒内能找到第一章、第一条命令和课程终点项目。

### P0-06 源码证据表

- [ ] 新增 `docs/source-evidence.md` 或每章内嵌“源码证据”表。
- [ ] 统一字段：教学结论、Pi 路径、符号/行号、固定 commit、Tau 对照、本地实验、已知简化。
- [ ] 将 `docs/pi-source-map.md` 从“路径列表”扩展为可查询索引。
- [ ] 生成 GitHub commit permalink，避免 `main` 分支漂移。
- [ ] 为每章标注“当前 Pi 行为 / 教学模型 / Tau 对照”三种证据级别。
- 验收：每一个带有“当前 Pi 行为”标题的断言都能追溯到固定源码位置。

---

## 2. P1：建立连续编码主线

> 目标：借鉴 `learn-agent-the-hard-way`，让读者从 60~100 行开始，逐步得到一个可运行 Harness。

### P1-01 新增主线目录

- [ ] 新增 `docs/12-build-a-harness/README.md`，作为贯穿式项目总览。
- [ ] 新增 `examples/harness/` 作为逐章演进的代码目录。
- [ ] 每一步保留独立可运行状态，不直接覆盖上一阶段。
- [ ] 每一步提供 `README.md`、完整实现、TODO 实现和测试。
- [ ] 在 `docs/00-course-map.md` 增加“机制路线”和“构建路线”双入口。

建议的 12 个步骤：

| 步骤 | 主题 | 最小产物 |
| --- | --- | --- |
| B01 | 裸 API 调用 | `chat_once.py` |
| B02 | messages 多轮历史 | `conversation.py` |
| B03 | 流式输出 | `streaming.py` |
| B04 | 第一个工具 | `read_file` |
| B05 | 工具注册表 | `ToolRegistry` |
| B06 | 工具结果与错误回填 | `ToolResult` |
| B07 | 权限与工作目录 | allow/ask/deny |
| B08 | JSONL 会话 | append-only transcript |
| B09 | 上下文预算与压缩 | compaction |
| B10 | 事件流与前端适配 | `subscribe()` |
| B11 | Provider / RPC | provider registry + JSONL |
| B12 | 组合成可评测 Harness | 完整项目 |

### P1-02 每一步采用“增量 diff”教学

- [ ] 每一步文档明确“上一阶段已有内容”和“本阶段新增内容”。
- [ ] 提供 `git diff` 或手写 diff，解释每个新增字段和函数的原因。
- [ ] 代码中保留简短注释，正文解释设计取舍，避免把所有说明塞进代码。
- [ ] 每个步骤增加一个故意失败的 TODO，例如缺失 tool result、未限制路径或错误未隔离。
- [ ] 每一步都能单独执行测试：`python3 -m unittest discover -s examples/harness/step_xx/tests`。
- 验收：学习者可只看当前步骤完成代码，不需要复制最终版本。

### P1-03 拆分 `mini_agent.py`

- [ ] 将 `learn_pi_lab/labs/mini_agent.py` 拆成：
  - `messages.py`
  - `tools.py`
  - `events.py`
  - `loop.py`
  - `providers.py`
  - `sessions/jsonl.py`
  - `context/compaction.py`
  - `harness.py`
- [ ] 保留一个小于 100 行的 `mini_agent.py` 入口，用于展示组合关系。
- [ ] 保持现有公共 API 或提供迁移兼容层，避免一次性破坏测试。
- [ ] 每个模块添加独立 doctest 或最小单元测试。
- 验收：初学者可以只打开一两个模块理解当前章节，不需要阅读 500 行文件。

### P1-04 增加失败实验矩阵

每个核心章节至少加入以下三类场景：

- [ ] 正常路径：工具调用成功并完成任务。
- [ ] 协议/参数错误：非法 JSON、缺字段、未知工具、截断响应。
- [ ] 安全边界：路径穿越、危险命令、超时、敏感错误泄露。

建议统一输出格式：

```text
模型请求 -> Harness 检查 -> 工具执行/拒绝 -> ToolResult -> 下一轮
```

- [ ] 每个失败实验说明“谁发现错误、错误是否回填模型、是否产生副作用”。
- [ ] 测试同时断言返回值、事件顺序和副作用状态。

---

## 3. P1：补齐内容深度和生产边界

### P1-05 流式响应与取消

- [ ] 新增章节或扩展 `docs/02-agent-loop/README.md`：delta 合并、半截响应、流式错误。
- [ ] 增加 AbortSignal/取消令牌的 Python 等价实验。
- [ ] 展示工具执行中取消、重试等待中取消和 Provider 流式中断的区别。
- [ ] 增加超时预算、总 deadline 和每个工具独立 timeout。
- [ ] 说明已输出部分如何展示、是否写入会话、如何避免重复。

### P1-06 工具系统深化

- [ ] 扩展 `docs/03-tools/README.md`：schema 校验、默认值、参数修复、版本化工具名。
- [ ] 增加工具元数据：只读/有副作用、是否幂等、是否允许并发、超时、敏感级别。
- [ ] 增加读工具并发、写工具串行的调度实验。
- [ ] 增加工具输出截断策略和结构化错误协议。
- [ ] 增加 tool result 中的 prompt injection 风险案例。

### P1-07 会话与上下文深化

- [ ] 增加 JSONL v3 完整记录类型表和版本迁移策略。
- [ ] 增加会话损坏恢复、重复追加、部分写入和并发读写实验。
- [ ] 增加 token 估算误差、硬上限、软上限和压缩触发阈值的对照。
- [ ] 增加压缩后工具调用成对完整性的性质测试。
- [ ] 增加敏感信息从历史、摘要和日志中传播的示例。

### P1-08 Provider 与真实协议

- [ ] 增加 OpenAI-compatible、Anthropic、Ollama 三种协议的最小 mock server。
- [ ] 将 provider metadata、协议 translator、认证配置拆成三层讲解。
- [ ] 增加 `Retry-After`、jitter、fallback 和错误分类实验。
- [ ] 增加真实 Provider 可选模式，默认仍为离线：
  - API Key 只从环境变量读取。
  - 命令显式带 `--live`。
  - 输出成本和网络提示。
  - 测试不依赖外网。

### P1-09 事件与前端适配

- [ ] 增加完整事件时间线：agent、turn、message、tool、retry、compaction、error。
- [ ] 展示同一事件流如何被 TUI、JSON 输出和 Web UI 三种消费者使用。
- [ ] 说明 `session.subscribe` 与扩展事件监听的边界。
- [ ] 增加事件版本化、未知事件兼容和订阅者异常隔离。

### P1-10 高级能力专题

按“可选扩展”分组，避免破坏核心课程节奏：

- [ ] steering / follow-up / abort。
- [ ] subagent：独立 history、工具注册表隔离、结果汇总。
- [ ] 多 subagent 并发、上限、取消和共享资源问题。
- [ ] MCP 客户端：协议边界、工具发现、权限绕过风险。
- [ ] skills：发现、触发、注入、版本和人工审批。
- [ ] 沙箱：路径边界、进程隔离、资源配额和逃逸限制。
- [ ] 浏览器工具：导航等待、CDP、异常翻译和输出截断。
- [ ] 后台任务：任务生命周期、完成通知和退出清理。

每个专题至少包含：设计图、最小离线模拟、一次失败实验、Pi/Tau 对照和安全限制。

---

## 4. P1：增加 TypeScript / Pi 对照轨道

### P1-11 双版本代码结构

- [ ] 每章增加“Python 教学模型”和“TypeScript/Pi 对照”两个 Tab。
- [ ] Python 代码保持标准库、离线、确定性。
- [ ] TypeScript 代码只展示 Pi 真实 API 的最小片段，不复制完整实现。
- [ ] 为每个对照标注：相同抽象、不同 API、教学简化、生产额外要求。
- [ ] 章节元数据增加 `python_example`、`typescript_example`、`pi_source` 字段。

### P1-12 Tau 对照规范

- [ ] 统一 Tau 链接到具体文件和符号，而不是只链接仓库首页。
- [ ] 为每章增加“本地模型 / Tau / Pi”三列对照表。
- [ ] 明确 Tau 是真实 Python 对照实现，不等于本课程的教学模型。
- [ ] 增加 Tau 版本或 commit 信息，避免引用漂移。

---

## 5. P1：贯穿式综合项目与评测

### P1-13 完整项目：Offline Coding Agent

- [ ] 新增 `projects/coding_agent/`，最终组合现有模块。
- [ ] 支持：多轮对话、read/search、受限写入、会话持久化、压缩、事件输出、权限策略。
- [ ] 所有默认工具使用课程安全边界，不执行用户任意 shell。
- [ ] 提供 CLI、JSONL RPC 和可选 Web 事件输出三种入口。
- [ ] 提供 `examples/fixtures/` 固定仓库和 10 个任务样例。
- [ ] 每个任务记录允许工具、预期副作用和禁止行为。

### P1-14 Agent 评测框架

- [ ] 新增 `evals/`，区分三层指标：
  - 功能正确性。
  - 工具安全性。
  - 结果质量。
- [ ] 输出稳定 JSON 报告，字段包括：任务、通过/失败、轮数、工具调用、错误类型、耗时、token（可选）。
- [ ] 增加回归门槛：禁止越权、禁止未授权写入、工具调用必须在允许列表。
- [ ] 提供人工评分字段，但不把人工评分伪装成确定性测试。
- [ ] 在 CI 中运行离线评测；真实模型评测单独手动触发。

---

## 6. P1/P2：页面排版和阅读体验

### P1-15 mdBook 主题重构

- [ ] 将 `theme/custom.css` 的视觉定位明确为“Agent 工程实验室”。
- [ ] 保留深色主题，同时完善浅色主题对比度和代码可读性。
- [ ] 减少蓝紫渐变，使用一个主强调色和一个状态色表达交互/实验状态。
- [ ] 正文宽度控制在 720-800px，代码块允许独立横向滚动。
- [ ] 标题、引用、表格、命令、警告和实验结果建立稳定的视觉层级。
- [ ] 卡片圆角控制在 8px 以内，避免页面全是浮动卡片。
- [ ] 增加 `:focus-visible`、高对比度和 reduced-motion 支持。

### P1-16 章节三栏布局

在 mdBook 能力允许的范围内实现：

- [ ] 左侧：章节分组和当前章节高亮。
- [ ] 中间：正文和代码。
- [ ] 右侧：当前页面标题大纲。
- [ ] 顶部：章节标题、预计时间、代码行数、阅读进度。
- [ ] 底部：上一章、下一章、对应实验和源码链接。
- [ ] 手机端：左侧目录改为可关闭抽屉，不占用正文宽度。

### P1-17 交互式图表

- [ ] 新增 `docs/assets/`，统一存放 SVG/Mermaid 源文件和导出图。
- [ ] 优先制作：
  - Agent Loop 时序图。
  - Trace/Turn 嵌套图。
  - 工具权限判定流程。
  - JSONL 会话树。
  - Compaction 前后消息边界。
  - Provider translator 链路。
  - Event stream 时间线。
  - RPC 粘包/半包处理。
- [ ] SVG 支持点击放大；图片提供 alt 文本和纯文本说明。
- [ ] 图表中的节点可链接到正文小节或代码行。

### P1-18 代码阅读组件

- [ ] 代码块增加文件名、语言、行号、复制、折叠、源码链接。
- [ ] 支持 Python / TypeScript 双 Tab。
- [ ] 支持“教学版 / Pi 生产对照”双 Tab。
- [ ] 为长代码保留“展开全部”状态，并存储在 localStorage。
- [ ] 复制按钮使用明确的成功/失败状态，不依赖颜色单独传达结果。
- [ ] 代码块不使用会导致 hydration mismatch 的 DOM 改写方式；若引入客户端脚本，增加页面切换测试。

### P1-19 学习状态和搜索

- [ ] 增加本地存储的章节完成状态。
- [ ] 在章节末尾提供勾选清单和“运行验证”按钮入口。
- [ ] 增加按主题搜索：Loop、工具、会话、上下文、Provider、安全、评测。
- [ ] 术语表支持页面内链接或 hover 释义。
- [ ] 不要求账号，不上传学习记录。

---

## 7. P2：发布、测试和维护能力

### P2-01 文档质量检查

- [ ] `check_course_contract.py` 检查每章必备小节和元数据。
- [ ] `check_markdown_links.py` 检查 docs、examples、projects、源码链接。
- [ ] 新增检查：代码块中的路径存在、运行命令可解析、章节交叉引用完整。
- [ ] 新增检查：所有“当前 Pi 行为”都有源码证据。
- [ ] 新增检查：Python/TypeScript 对照章节成对存在。

### P2-02 测试分层

- [ ] 单元测试：纯函数、消息、权限、会话、压缩、编解码。
- [ ] 契约测试：事件顺序、工具结果配对、JSONL 版本。
- [ ] 集成测试：完整 Harness 离线任务。
- [ ] 页面测试：站点构建、代码复制、折叠、Tab、手机布局。
- [ ] 安全测试：路径穿越、危险命令、敏感错误、提示词注入、资源上限。
- [ ] 维持 `python3 -m unittest discover -s tests -v` 全绿作为最低门槛。

### P2-03 发布物

- [ ] 发布静态 HTML。
- [ ] 发布 PDF，并确保代码块、图表和中文字体可读。
- [ ] 发布离线压缩包，包含 docs、实验、示例和测试。
- [ ] README 增加版本号、Pi 基线 commit、最近更新日期和兼容性说明。
- [ ] 每次更新 Pi 基线时，必须同时更新源码映射、章节、对照代码和契约测试。

### P2-04 贡献者体验

- [ ] 新增 `CONTRIBUTING.md`：新增章节、代码示例、图表、测试、链接检查流程。
- [ ] 提供章节模板文件 `docs/_templates/chapter.md`。
- [ ] 提供示例任务模板和评测 fixture 模板。
- [ ] 规定文档中文术语、英文 API 名称和代码注释风格。
- [ ] 规定所有代码示例不得包含真实 API Key、个人路径或不可复现网络结果。

---

## 8. 推荐实施顺序

### 第 1 周：基线修复

- [ ] P0-01 终止语义修正。
- [ ] P0-02 章节模板。
- [ ] P0-03 构建和 CI。
- [ ] P0-04 代码块基础增强。
- [ ] P0-05 首页重做。

### 第 2-3 周：连续主线

- [ ] P1-01 主线目录和 B01-B06。
- [ ] P1-03 拆分 `mini_agent.py`。
- [ ] P1-04 失败实验矩阵。
- [ ] 为现有 11 章补齐源码证据表。

### 第 4-6 周：生产边界

- [ ] P1-05 流式、取消、超时。
- [ ] P1-06 工具元数据和调度。
- [ ] P1-07 会话损坏恢复和压缩性质测试。
- [ ] P1-08 mock Provider 和 fallback。
- [ ] P1-09 事件流消费者。

### 第 7-9 周：高级能力和综合项目

- [ ] P1-10 subagent、skills、MCP、沙箱、浏览器、后台任务。
- [ ] P1-11 TypeScript/Pi 对照。
- [ ] P1-13 完整 Offline Coding Agent。
- [ ] P1-14 离线 Agent 评测。

### 第 10-12 周：页面与发布

- [ ] P1-15 至 P1-19 页面体验。
- [ ] P2-01 至 P2-03 检查、测试、HTML/PDF/离线包。
- [ ] 进行桌面 1440px、平板 1024px、手机 390px 三档验收。

---

## 9. 每项改造的完成定义

一个任务只有同时满足以下条件才算完成：

- [ ] 文档解释了问题、机制、取舍和边界。
- [ ] 至少有一个可运行示例。
- [ ] 至少有一个失败/安全案例。
- [ ] 有对应单元测试或集成测试。
- [ ] 有 Pi 源码、Tau 或明确的“教学模型”证据标注。
- [ ] 有运行命令和预期输出。
- [ ] 页面在浅色、深色、桌面和移动端可读。
- [ ] 通过课程契约、链接检查和站点构建。

## 10. 最终验收清单

- [ ] 新用户能在 10 分钟内跑通第一个 Agent Loop 实验。
- [ ] 学习者能沿连续主线从裸 API 写到完整 Harness。
- [ ] 每个核心机制都能在 Python、Pi TypeScript 或 Tau 中找到对照。
- [ ] 读者能看到正常路径、错误路径和安全拒绝路径。
- [ ] 课程包含至少一个完整离线项目和一套离线 Agent 评测。
- [ ] 章节页面有清晰目录、代码增强、图表和前后导航。
- [ ] 构建、测试、链接和文档契约可由 CI 自动完成。
- [ ] Pi 基线、版本、源码链接和已知简化均有明确记录。

